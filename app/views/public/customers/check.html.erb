<h3>customers#check</h3>

<div>
  <div>
    <h2>本当に退会しますか？</h2>
    <h3>退会すると、会員登録情報や</br>
    これまでの購入履歴が閲覧できなくなります。</br>
    退会する場合は、「退会する」をクリックしてください。</h3>
  </div>

  <div>
    <%= link_to '退会しない', edit_customers_path %>
  </div>
  <div>
    <%= form_with(model: @customer, url: withdraw_path(@customer), method: :patch) do |f| %>
      <%= f.submit "退会する" %>
    <% end %>
  </div>
</div>